package com.jbksoft.test1;

import java.util.ArrayList;
import java.util.Iterator;

public class AA {
    public static void main(String[] args) {
        ArrayList<User> userList=new ArrayList<User>();

       /* UserGroup group1=new UserGroup();
        group1.serviceName="s1";


        UserGroup group2=new UserGroup();
        group2.serviceName="s2";

        UserGroup group3=new UserGroup();
        group3.serviceName="s3";

        group1.register(group2);
        group2.register(group1);*/
        //group3.register(group1);

      /*  User user=new User("Pooja",1);
        group1.addUser(user);

        User user1=new User("Pooja1",2);
        group2.addUser(user1);

        //User user2=new User("Pooja1",1);
        //group2.addUser(user2);
        //userList.add(user);

        group2.print();
*/


       /* if(userList.contains(user1)){
           int a= userList.indexOf(user);
           userList.set(a,user1);
        }else{
            userList.add(user1);
        }

        System.out.println(userList);*/
        /*userList

        User user1=new User("Pooja1",1);
        userList.add(user1);*/


       /* User user2=new User("Pooja1",2);
        userList.add(user2);

        System.out.println("---"+userList);
        System.out.println();

        if(userList.contains(user2)) {
            Iterator<User> iter = userList.iterator();

            while (iter.hasNext()) {
                User str = iter.next();

                if (str.equals(user2))
                    iter.remove();
            }
        }

        System.out.println(userList);

        if(!userList.contains(user2)){
           System.out.println("False");
        }*/

    }
}
